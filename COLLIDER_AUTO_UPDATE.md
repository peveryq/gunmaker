# Автоматическое обновление коллайдеров при спавне деталей

## 🎯 Проблема

При использовании универсального префаба Part с динамической подстановкой мешей возникала проблема:
- Коллайдер настроен под исходный пустой меш
- При подстановке нового меша коллайдер не обновлялся
- Размеры коллайдера не соответствовали геометрии детали

## ✅ Решение

Добавлен автоматический пересчет коллайдера после подстановки меша в `PartSpawner.cs`.

---

## 🔧 Реализация

### Метод UpdateCollider

```csharp
private void UpdateCollider(GameObject obj, Mesh mesh)
{
    // Проверяет тип коллайдера и обновляет его параметры
    // на основе bounds нового меша
}
```

### Поддерживаемые типы коллайдеров:

#### 1. **MeshCollider** (рекомендуется для точности)
```csharp
meshCollider.sharedMesh = null;  // Сброс
meshCollider.sharedMesh = mesh;  // Новый меш
```
- ✅ Точно повторяет геометрию меша
- ⚠️ Более затратный по производительности
- 💡 Используйте для сложных форм

#### 2. **BoxCollider** (рекомендуется для простоты)
```csharp
boxCollider.center = mesh.bounds.center;
boxCollider.size = mesh.bounds.size;
```
- ✅ Быстрый и эффективный
- ✅ Подходит для большинства деталей
- 💡 Лучший выбор для weapon parts

#### 3. **CapsuleCollider**
```csharp
capsuleCollider.center = mesh.bounds.center;
capsuleCollider.height = mesh.bounds.size.y;
capsuleCollider.radius = Max(size.x, size.z) * 0.5f;
```
- ✅ Хорошо для цилиндрических деталей
- 💡 Подходит для стволов, магазинов

#### 4. **SphereCollider**
```csharp
sphereCollider.center = mesh.bounds.center;
sphereCollider.radius = Max(size.x, size.y, size.z) * 0.5f;
```
- ✅ Самый быстрый
- ⚠️ Неточный для большинства деталей
- 💡 Используйте только для округлых объектов

---

## 📋 Порядок работы

```
1. SpawnPart() вызывается с mesh
   ↓
2. Instantiate универсального префаба
   ↓
3. Подстановка mesh в MeshFilter
   ↓
4. UpdateCollider(obj, mesh) ← НОВОЕ!
   ├─ Определение типа коллайдера
   ├─ Расчет bounds меша
   └─ Обновление параметров коллайдера
   ↓
5. Применение stats
   ↓
6. Спавн с физикой
```

---

## 🎮 Рекомендации по настройке Part префаба

### Вариант 1: BoxCollider (рекомендуется)
```
Part (GameObject)
  ├─ MeshFilter
  ├─ MeshRenderer
  ├─ BoxCollider ← размеры будут автоматически обновлены
  ├─ WeaponPart
  └─ ...
```
**Плюсы:**
- ✅ Быстрая работа
- ✅ Подходит для 90% деталей
- ✅ Автоматически подгоняется под меш

**Настройка:**
- Начальные значения не важны (будут перезаписаны)
- Можно оставить default (0,0,0) center и (1,1,1) size

### Вариант 2: MeshCollider (для точности)
```
Part (GameObject)
  ├─ MeshFilter
  ├─ MeshRenderer
  ├─ MeshCollider ← меш будет автоматически обновлен
  │   └─ Convex: true (если нужна физика)
  ├─ WeaponPart
  └─ ...
```
**Плюсы:**
- ✅ Максимальная точность
- ✅ Идеально повторяет форму

**Минусы:**
- ⚠️ Медленнее BoxCollider
- ⚠️ Больше нагрузка на физику

**Настройка:**
- Оставьте Shared Mesh пустым (будет заполнен автоматически)
- Включите Convex если нужно Rigidbody взаимодействие

---

## 🧪 Тестирование

### Как проверить что коллайдер обновился:

1. **В редакторе:**
   - Купите деталь в магазине
   - Найдите spawned объект в Hierarchy
   - Выберите его
   - В Scene view включите коллайдеры (кнопка наверху)
   - Коллайдер должен соответствовать визуальной геометрии

2. **В игре:**
   - Купите деталь
   - Попробуйте взаимодействовать с ней (подбор)
   - Коллайдер должен срабатывать на видимой геометрии
   - Не должно быть "пустот" или слишком больших зон

### Отладка:

Если коллайдер не обновляется:

1. Проверьте что в Part префабе есть один из поддерживаемых коллайдеров
2. Посмотрите Console на предупреждение:
   ```
   "No collider found on PartName"
   ```
3. Добавьте BoxCollider в префаб и попробуйте снова

---

## 📊 Сравнение типов коллайдеров

| Тип | Точность | Производительность | Использование |
|-----|----------|-------------------|---------------|
| **BoxCollider** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | Рекомендуется для деталей оружия |
| **MeshCollider** | ⭐⭐⭐⭐⭐ | ⭐⭐ | Для сложных форм |
| **CapsuleCollider** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | Для стволов, цилиндров |
| **SphereCollider** | ⭐⭐ | ⭐⭐⭐⭐⭐ | Не рекомендуется |

---

## 💡 Примеры использования

### Пример 1: Ствол (длинная деталь)
```
Mesh bounds: (0, 0, 0) center, (0.5, 0.5, 2.0) size

BoxCollider после update:
  center = (0, 0, 0)
  size = (0.5, 0.5, 2.0) ← идеально обволакивает
```

### Пример 2: Магазин (короткий прямоугольник)
```
Mesh bounds: (0, -0.5, 0) center, (0.3, 1.0, 0.8) size

BoxCollider после update:
  center = (0, -0.5, 0)
  size = (0.3, 1.0, 0.8) ← точно под меш
```

### Пример 3: Приклад (сложная форма)
Если нужна высокая точность:
```
Используйте MeshCollider
sharedMesh = partMesh ← копирует геометрию
```

---

## ⚙️ Дополнительные настройки

### Тонкая настройка BoxCollider

Если автоматический размер не идеален, можно добавить скрипт настройки:

```csharp
// После UpdateCollider можно добавить коррекцию
BoxCollider box = obj.GetComponent<BoxCollider>();
if (box != null)
{
    // Увеличить на 10% для лучшего подбора
    box.size *= 1.1f;
    
    // Или сдвинуть center
    box.center += new Vector3(0, 0.1f, 0);
}
```

### Игнорирование обновления

Если не хотите автообновление для конкретного префаба:
```csharp
// Добавьте тег "NoColliderUpdate" на префаб
if (obj.CompareTag("NoColliderUpdate"))
{
    return; // Skip update
}
```

---

## 🚀 Производительность

### Влияние на FPS:

**UpdateCollider вызывается:**
- ✅ Только при спавне детали (не каждый кадр)
- ✅ Один раз на деталь
- ✅ Очень быстрая операция (~0.01ms)

**Нагрузка:**
- BoxCollider update: ~0.001ms
- MeshCollider update: ~0.05ms

**Вывод:** Нулевое влияние на производительность игры ✅

---

## ✅ Результат

Теперь при покупке детали в магазине:

1. ✅ Спавнится универсальный префаб
2. ✅ Подставляется правильный меш
3. ✅ **Коллайдер автоматически пересчитывается** ← НОВОЕ!
4. ✅ Применяются характеристики
5. ✅ Деталь готова к использованию

**Коллайдер всегда соответствует визуальной геометрии детали!** 🎯

