# Состояния GameObject в магазине - Четкая инструкция

## ✅ Что должно быть ОТКЛЮЧЕНО по умолчанию

### 1. PurchaseOverlay (ТОЛЬКО ЭТО!)
```
Hierarchy: Canvas → ShopPanel → PurchaseOverlay
Состояние: ❌ ОТКЛЮЧЕН (галочка снята)
Причина: Модальное окно покупки не должно быть видно пока не кликнули на плитку
```

**Как отключить:**
1. Выберите PurchaseOverlay в Hierarchy
2. Снимите галочку в Inspector
3. PurchaseOverlay станет серым

---

## ✅ Что должно быть ВКЛЮЧЕНО

### Все остальное!

```
Canvas → ✅ ВКЛЮЧЕН
├─ EventSystem → ✅ ВКЛЮЧЕН
├─ ShopPanel → ✅ ВКЛЮЧЕН (код сам скроет при старте!)
│   ├─ CategoriesArea → ✅ ВКЛЮЧЕН
│   ├─ HeaderBar → ✅ ВКЛЮЧЕН
│   ├─ GridArea → ✅ ВКЛЮЧЕН
│   └─ PurchaseOverlay → ❌ ОТКЛЮЧЕН (единственный отключенный!)
│       └─ PurchaseModal → ✅ ВКЛЮЧЕН (внутри PurchaseOverlay)
│
└─ (другие UI элементы) → ✅ ВКЛЮЧЕНЫ

MoneySystem → ✅ ВКЛЮЧЕН
ShopOfferingGenerator → ✅ ВКЛЮЧЕН
PartSpawner → ✅ ВКЛЮЧЕН
ShopComputer → ✅ ВКЛЮЧЕН
Player → ✅ ВКЛЮЧЕН
```

---

## 🎯 Важно понимать!

### Разница между GameObject и Component:

**GameObject отключен (галочка снята):**
- ❌ Не виден в игре
- ❌ Awake() НЕ вызывается
- ❌ Start() НЕ вызывается
- ❌ Update() НЕ вызывается
- ✅ Можно включить через скрипт: `SetActive(true)`

**Component отключен (галочка на компоненте снята):**
- ✅ GameObject активен
- ✅ Awake() вызывается
- ✅ Start() вызывается
- ❌ Update() НЕ вызывается (если компонент отключен)
- ✅ Можно включить через скрипт: `component.enabled = true`

---

## 📊 Проверка правильности настройки

### В Hierarchy должно быть:

```
Canvas (белый) ✅
├─ EventSystem (белый) ✅
└─ ShopPanel (белый) ✅ ← ВКЛЮЧЕН! Код сам скроет
    ├─ CategoriesArea (белый) ✅
    ├─ HeaderBar (белый) ✅
    ├─ GridArea (белый) ✅
    └─ PurchaseOverlay (серый) ❌ ← ЕДИНСТВЕННЫЙ ОТКЛЮЧЕННЫЙ!
        └─ PurchaseModal (белый внутри серого родителя) ✅
```

**Цвета в Hierarchy:**
- **Белый** = GameObject включен
- **Серый** = GameObject отключен

---

## 🔍 Как это работает

### При запуске игры:

1. **MoneySystem, ShopOfferingGenerator, PartSpawner**
   - ✅ Активны и инициализируются в Awake()

2. **ShopPanel (включен в Inspector)**
   - ✅ ShopUI.Awake() вызывается
   - ✅ Находит FirstPersonController
   - ✅ Настраивает все кнопки
   - ✅ Отключает shopPanel программно: `shopPanel.SetActive(false)`
   - ✅ Теперь не виден, но полностью инициализирован
   - ✅ Готов к открытию через `ShopUI.OpenShop()`

3. **PurchaseOverlay (отключен)**
   - ❌ Не виден
   - ❌ PurchaseConfirmationUI.Awake() НЕ вызывается
   - ✅ InitializeListeners() вызовется при первом ShowPurchaseConfirmation()

### Когда игрок нажимает E на ShopComputer:

1. `ShopComputer.Interact()` вызывает `ShopUI.OpenShop()`
2. `ShopUI.OpenShop()` делает `shopPanel.SetActive(true)`
3. ShopPanel становится видимым
4. Все дочерние элементы (которые были включены) становятся видимыми

### Когда игрок кликает на плитку:

1. `ShopItemTile` вызывает `PurchaseConfirmationUI.ShowPurchaseConfirmation()`
2. `ShowPurchaseConfirmation()` вызывает `InitializeListeners()` (инициализация кнопок)
3. `ShowPurchaseConfirmation()` делает `overlay.SetActive(true)` и `modalPanel.SetActive(true)`
4. Модальное окно становится видимым

---

## ⚠️ Типичные ошибки

### ❌ Ошибка 1: Canvas отключен
**Симптом:** Вообще ничего не видно в UI
**Решение:** Включите Canvas

### ❌ Ошибка 2: EventSystem отключен
**Симптом:** Кнопки не реагируют на клики
**Решение:** Включите EventSystem

### ❌ Ошибка 3: ShopPanel отключен в Inspector
**Симптом:** Магазин не открывается, управление блокируется
**Решение:** ВКЛЮЧИТЕ ShopPanel (код сам его скроет при старте)

### ❌ Ошибка 4: PurchaseOverlay включен
**Симптом:** Модальное окно видно всегда
**Решение:** Отключите PurchaseOverlay

### ❌ Ошибка 5: Дочерние элементы отключены
**Симптом:** В магазине нет кнопок/элементов
**Решение:** Включите все дочерние элементы внутри ShopPanel

---

## ✅ Финальный чеклист

- [ ] Canvas включен
- [ ] EventSystem включен
- [ ] ShopPanel **ВКЛЮЧЕН** (код сам скроет!)
- [ ] CategoriesArea включен (внутри ShopPanel)
- [ ] HeaderBar включен (внутри ShopPanel)
- [ ] GridArea включен (внутри ShopPanel)
- [ ] PurchaseOverlay **ОТКЛЮЧЕН** (единственный!)
- [ ] PurchaseModal включен (внутри PurchaseOverlay)
- [ ] ModalCloseButton включен (внутри PurchaseModal)
- [ ] BuyButton включен (внутри PurchaseModal)
- [ ] MoneySystem GameObject включен
- [ ] ShopOfferingGenerator GameObject включен
- [ ] PartSpawner GameObject включен
- [ ] ShopComputer включен

---

## 🎮 Быстрая проверка в редакторе

### Метод 1: Визуальная проверка в PlayMode
1. Запустите игру в PlayMode
2. ShopPanel НЕ должен быть виден (код скрыл его)
3. PurchaseOverlay не должен быть виден

### Метод 2: Проверка в Hierarchy ДО PlayMode
1. ShopPanel должен быть БЕЛЫМ (включен)
2. PurchaseOverlay должен быть СЕРЫМ (отключен)
3. Все остальное белым

### Метод 3: Проверка через скрипт
В ShopUI.Awake() уже есть код который скрывает панель:
```csharp
shopPanel.SetActive(false); // Скрывает панель программно
```

---

## 🔧 Если что-то не так

**Если ShopPanel случайно отключен:**
```
1. Выберите ShopPanel
2. ВКЛЮЧИТЕ галочку в Inspector
3. Сохраните сцену (Ctrl+S)
```

**Если PurchaseOverlay случайно включен:**
```
1. Выберите PurchaseOverlay
2. Снимите галочку в Inspector
3. Сохраните сцену (Ctrl+S)
```

**Если после этого магазин не открывается:**
```
Проверьте Console на ошибки при запуске игры
```

---

## 📝 Итого

**Правило простое:**
- **ТОЛЬКО PurchaseOverlay отключен** (модальное окно покупки)
- **ВСЕ ОСТАЛЬНОЕ ВКЛЮЧЕНО!**

**Почему ShopPanel включен?**
- ShopUI.Awake() должен выполниться для инициализации
- Код сам скрывает панель через `shopPanel.SetActive(false)`
- При открытии магазина код показывает панель через `shopPanel.SetActive(true)`

**Отключенный GameObject:**
1. ❌ PurchaseOverlay (откроется при клике на плитку)

**Включенные GameObject:**
1. ✅ ShopPanel (код сам скроет при старте)
2. ✅ Все остальное!

