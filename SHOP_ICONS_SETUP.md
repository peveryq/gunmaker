# 🖼️ Настройка иконок деталей в магазине

## 📋 Обзор изменений

Теперь система магазина поддерживает отображение PNG иконок для каждой детали:
- **На плитках** товаров в магазине
- **В модальном окне** подтверждения покупки
- **Привязка** меш → спрайт через инспектор Unity

---

## 🏗️ Архитектура

### Новый класс: `PartMeshData`

Связывает меш детали с иконкой:

```csharp
public class PartMeshData
{
    public Mesh mesh;    // FBX меш детали
    public Sprite icon;  // PNG спрайт для UI
}
```

### Обновленная структура `RarityTier`

Вместо:
```csharp
public List<Mesh> partMeshes;
```

Теперь:
```csharp
public List<PartMeshData> partMeshData;
```

### Обновленная структура `ShopOffering`

Добавлено поле:
```csharp
public Sprite partIcon; // Иконка для UI
```

---

## 🎯 Настройка в Unity Editor

### Шаг 1: Подготовка PNG спрайтов

1. **Импортируйте PNG иконки** в папку проекта (например, `Assets/Icons/Parts/`)

2. **Настройте каждую PNG иконку:**
   - Выберите PNG в Project Window
   - В Inspector:
     - **Texture Type**: `Sprite (2D and UI)`
     - **Sprite Mode**: `Single`
     - **Pixels Per Unit**: `100` (или по вашему дизайну)
     - **Filter Mode**: `Bilinear`
     - **Max Size**: `512` или `1024`
     - Нажмите **Apply**

### Шаг 2: Настройка ShopPartConfig

1. **Откройте** `ShopPartConfig` ScriptableObject в Inspector

2. **Для каждого типа детали** (Barrel, Magazine, Stock, Scope):

3. **Для каждого уровня редкости** (Rarity 1-5):
   
   **Раскройте секцию "Part Meshes & Icons":**
   
   - **Size**: Количество вариантов деталей для этой редкости
   
   - **Для каждого элемента:**
     - **Element N**:
       - **Mesh**: Перетащите FBX меш детали
       - **Icon**: Перетащите PNG спрайт иконки
   
   #### Пример для Barrel Rarity 1:
   ```
   Part Meshes & Icons:
     Size: 3
     Element 0:
       Mesh: Barrel_Basic_01
       Icon: Icon_Barrel_Basic_01
     Element 1:
       Mesh: Barrel_Basic_02
       Icon: Icon_Barrel_Basic_02
     Element 2:
       Mesh: Barrel_Basic_03
       Icon: Icon_Barrel_Basic_03
   ```

### Шаг 3: Проверка

**Запустите игру и откройте магазин:**

✅ **На плитках товаров** должны отображаться иконки деталей
✅ **В модальном окне** должна отображаться иконка выбранной детали
✅ **При покупке** должна спауниться деталь с соответствующим мешем

---

## 🎨 Рекомендации по дизайну иконок

### Размер и формат
- **Разрешение**: 256x256 или 512x512 пикселей
- **Формат**: PNG с прозрачностью (alpha channel)
- **Соотношение**: 1:1 (квадратные)

### Стиль
- **Фон**: Прозрачный
- **Ракурс**: Изометрия или вид сбоку (единообразно для всех)
- **Освещение**: Мягкое, нейтральное
- **Цвет**: Соответствует 3D мешу

### Именование
Рекомендуемая схема именования:
```
Icon_[PartType]_[Rarity]_[Number].png

Примеры:
- Icon_Barrel_Common_01.png
- Icon_Barrel_Common_02.png
- Icon_Magazine_Rare_01.png
- Icon_Stock_Epic_01.png
```

---

## 🔄 Процесс добавления новой детали

### 1. Создайте FBX меш детали
### 2. Создайте PNG иконку детали
### 3. Импортируйте оба файла в Unity
### 4. Настройте PNG как Sprite (см. Шаг 1)
### 5. Откройте ShopPartConfig
### 6. Выберите тип детали и редкость
### 7. Увеличьте Size в "Part Meshes & Icons"
### 8. Добавьте новый элемент:
   - Mesh = ваш FBX меш
   - Icon = ваш PNG спрайт

---

## 🛠️ Технические детали

### Где отображаются иконки

**1. ShopItemTile (плитка товара):**
```csharp
if (partIconImage != null && offering.partIcon != null)
{
    partIconImage.sprite = offering.partIcon;
    partIconImage.enabled = true;
}
```

**2. PurchaseConfirmationUI (модальное окно):**
```csharp
if (partIconImage != null && currentOffering.partIcon != null)
{
    partIconImage.sprite = currentOffering.partIcon;
    partIconImage.enabled = true;
}
```

### Как происходит генерация

**ShopOfferingGenerator.GenerateOffering():**
1. Выбирается случайная редкость (1-5)
2. Выбирается случайная цена в диапазоне редкости
3. Выбирается случайный `PartMeshData` из списка редкости
4. Из `PartMeshData` извлекаются:
   - `mesh` → используется для спавна детали
   - `icon` → используется для отображения в UI

---

## ✅ Чеклист настройки

### Для каждого типа детали (4 типа):
- [ ] Barrel
- [ ] Magazine
- [ ] Stock
- [ ] Scope

### Для каждой редкости (5 уровней):
- [ ] Rarity 1 (Common)
- [ ] Rarity 2 (Uncommon)
- [ ] Rarity 3 (Rare)
- [ ] Rarity 4 (Epic)
- [ ] Rarity 5 (Legendary)

### Для каждого элемента:
- [ ] FBX меш назначен
- [ ] PNG спрайт назначен
- [ ] Спрайт правильно импортирован (Texture Type: Sprite)

### Итого:
- 4 типа × 5 редкостей = **20 категорий**
- Если по 3 варианта в каждой = **60 деталей**
- Значит **60 мешей** + **60 иконок**

---

## 🐛 Troubleshooting

### Иконки не отображаются

**Проблема:** Иконка не видна на плитке/в модальном окне

**Решения:**
1. Проверьте, что PNG импортирована как **Sprite (2D and UI)**
2. Проверьте, что спрайт назначен в `PartMeshData.icon` в ShopPartConfig
3. Проверьте, что `Image` компонент `partIconImage` присутствует в UI

### Неправильная иконка для детали

**Проблема:** Спавнится одна деталь, а иконка от другой

**Решение:**
- Проверьте правильность назначения в `PartMeshData`:
  ```
  Element 0:
    Mesh: Barrel_01  ← должен соответствовать
    Icon: Icon_Barrel_01  ← этой иконке
  ```

### Иконки размытые

**Проблема:** Иконки выглядят нечетко

**Решения:**
1. Увеличьте **Max Size** спрайта (512 → 1024)
2. Используйте **Filter Mode: Bilinear** (или Trilinear)
3. Создайте PNG большего разрешения (512x512)

### Иконки обрезаются

**Проблема:** Часть иконки не видна

**Решения:**
1. В UI: проверьте **Image Type** → `Simple` (не Filled/Sliced)
2. Проверьте **Preserve Aspect** (может потребоваться включить/выключить)
3. Убедитесь, что `RectTransform` плитки достаточно большой

---

## 📝 Итоговый код (изменения)

### ShopPartConfig.cs
```csharp
// Новый класс
public class PartMeshData
{
    public Mesh mesh;
    public Sprite icon;
}

// Обновленный RarityTier
public class RarityTier
{
    public List<PartMeshData> partMeshData; // было: List<Mesh> partMeshes
    // ... остальные поля
}
```

### ShopOfferingGenerator.cs
```csharp
public class ShopOffering
{
    public Mesh partMesh;
    public Sprite partIcon; // новое поле
    // ... остальные поля
}

private ShopOffering GenerateOffering(...)
{
    PartMeshData meshData = tier.partMeshData[Random.Range(0, tier.partMeshData.Count)];
    
    return new ShopOffering
    {
        partMesh = meshData.mesh,
        partIcon = meshData.icon, // новое
        // ...
    };
}
```

### ShopItemTile.cs
```csharp
if (partIconImage != null && offering.partIcon != null)
{
    partIconImage.sprite = offering.partIcon;
    partIconImage.enabled = true;
}
```

### PurchaseConfirmationUI.cs
```csharp
if (partIconImage != null && currentOffering.partIcon != null)
{
    partIconImage.sprite = currentOffering.partIcon;
    partIconImage.enabled = true;
}
```

---

## 🎉 Готово!

Теперь в вашем магазине будут отображаться красивые иконки деталей! 🖼️✨

Каждая деталь имеет:
- ✅ 3D меш для спавна в мире
- ✅ 2D иконку для отображения в UI
- ✅ Автоматическую привязку через `PartMeshData`

---

## 📞 Следующие шаги

После настройки всех иконок рекомендую:

1. **Протестировать** каждую категорию деталей
2. **Проверить** соответствие иконок и мешей
3. **Отбалансировать** редкость и цены
4. **Добавить** звуки покупки (уже реализовано)

Удачи! 🚀

