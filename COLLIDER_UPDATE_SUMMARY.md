# Коллайдер: Автоматическое обновление - Краткая заметка

## ✅ Что исправлено

При спавне деталей через универсальный префаб коллайдер теперь **автоматически обновляется** под геометрию нового меша.

---

## 🔧 Изменения в коде

### Файл: `PartSpawner.cs`

**Добавлен метод:**
```csharp
private void UpdateCollider(GameObject obj, Mesh mesh)
```

**Что делает:**
1. Определяет тип коллайдера (BoxCollider, MeshCollider, и т.д.)
2. Пересчитывает параметры на основе `mesh.bounds`
3. Обновляет коллайдер под новую геометрию

**Где вызывается:**
```csharp
// После подстановки меша
meshFilter.mesh = partMesh;
UpdateCollider(spawnedPart, partMesh); // ← Вызов
```

---

## 📋 Поддерживаемые коллайдеры

| Тип | Что обновляется | Рекомендация |
|-----|----------------|--------------|
| **BoxCollider** | center, size | ✅ Лучший выбор |
| **MeshCollider** | sharedMesh | ✅ Для сложных форм |
| **CapsuleCollider** | center, height, radius | ✅ Для стволов |
| **SphereCollider** | center, radius | ⚠️ Неточный |

---

## 🎯 Как использовать

### В Part префабе добавьте коллайдер:

**Рекомендуется:**
```
Part.prefab
  ├─ BoxCollider (начальные значения не важны)
  └─ ... другие компоненты
```

**Или для точности:**
```
Part.prefab
  ├─ MeshCollider (convex = true, sharedMesh пустой)
  └─ ... другие компоненты
```

### Всё! 🎉

Коллайдер будет автоматически обновляться при каждом спавне.

---

## 🧪 Проверка

**В Scene view:**
- Включите отображение коллайдеров
- Купите деталь
- Коллайдер должен обволакивать меш

**В игре:**
- Детали корректно подбираются
- Взаимодействие работает правильно

---

## 📖 Подробная документация

См. `COLLIDER_AUTO_UPDATE.md` для полной информации.

---

✅ **Готово!** Коллайдеры теперь автоматически подстраиваются под любой меш.

